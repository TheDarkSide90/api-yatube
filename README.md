# api_yatube
api_yatube

Проект с тестовым API для авторов

Клонировать репозиторий и перейти в него в командной строке:

```bash
git clone https://github.com/TheDarkSide90/api-yatube.git
```

```bash
cd api-yatube
```

Cоздать и активировать виртуальное окружение:

```bash
python -m venv env
```

Перейти в рабочую папку:

```bash
cd yatube_api
```

Запустить проект:

```bash
python manage.py runserver
```
